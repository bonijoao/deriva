% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advance.R
\name{advance}
\alias{advance}
\alias{advance.drift_detector_fit}
\title{Advance a fitted drift detector over a new batch}
\usage{
advance(object, ...)

\method{advance}{drift_detector_fit}(object, new_data, ...)
}
\arguments{
\item{object}{A \code{drift_detector_fit}.}

\item{...}{Passed to methods.}

\item{new_data}{A data frame with the new batch, in temporal order,
containing the same signal column used in \code{\link[=fit]{fit()}}.}
}
\value{
A new \code{drift_detector_fit}.
}
\description{
Feeds a new batch of observations (any size, including 1 — stream mode)
to the detector and returns a NEW fitted object with the engine state
advanced and the annotated batch appended to the history. The original
object is not modified. This is the only way to persist state; see
\code{\link[generics:augment]{generics::augment()}} for a read-only preview.
}
\details{
Why not \code{update()}: in the tidymodels ecosystem \code{update()} on a spec
means "change hyperparameters", so deriva defines its own verb.
}
\examples{
base <- sim_drift_stream(n_pre = 100, n_post = 0, seed = 1)
f0 <- fit(drift_detector("ddm"), base, signal = error)
f1 <- advance(f0, sim_drift_stream(n_pre = 0, n_post = 50, seed = 2))
}
