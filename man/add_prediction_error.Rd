% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge.R
\name{add_prediction_error}
\alias{add_prediction_error}
\title{Build a drift signal from model predictions}
\usage{
add_prediction_error(data, truth, estimate = NULL, ...)
}
\arguments{
\item{data}{A data frame with truth and prediction columns.}

\item{truth}{Unquoted name of the true outcome column.}

\item{estimate}{Unquoted name of the prediction column. Defaults to
\code{.pred_class} (classification) or \code{.pred} (regression), following
tidymodels conventions.}

\item{...}{Not used.}
}
\value{
\code{data} as a tibble with a \code{.error} column added.
}
\description{
Bridge from tidymodels: takes the output of \code{augment()} on a fitted
workflow/model and adds a \code{.error} column — the signal drift detectors
consume. Classification (factor/character truth): 0/1 mismatch against
\code{estimate} (default column \code{.pred_class}). Regression (numeric truth):
absolute error against \code{estimate} (default column \code{.pred}).
}
\examples{
d <- tibble::tibble(truth = c(1, 2, 3), .pred = c(1, 1, 5))
add_prediction_error(d, truth = truth)
}
