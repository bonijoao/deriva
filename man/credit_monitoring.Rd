% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{credit_monitoring}
\alias{credit_monitoring}
\title{Simulated credit monitoring stream}
\format{
A tibble with 1,000 rows and 3 columns:
\describe{
\item{t}{Observation index, 1 to 1000.}
\item{error}{0/1 classifier error for that observation.}
\item{drift_true}{Ground truth: \code{TRUE} from observation 501 on,
the point the market shift occurred.}
}
}
\source{
Simulated with \code{\link[=sim_drift_stream]{sim_drift_stream()}}:
\code{sim_drift_stream(n_pre = 500, n_post = 500, p_pre = 0.05, p_post = 0.30, seed = 2)}.
See \code{data-raw/credit-monitoring.R}.
}
\usage{
credit_monitoring
}
\description{
A synthetic per-observation error stream from a credit-approval
classifier in production: 500 stable observations (5\% error rate),
then 500 after a market shift raised the error rate to 30\%. Frozen
with a fixed seed, so every example that loads it sees the same
story — including the single, correct DDM detection at \code{t = 542}
with no false positives before it.
}
\examples{
credit_monitoring
detect_drift(credit_monitoring, .col = error, method = "ddm")
}
\keyword{datasets}
