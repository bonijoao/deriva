% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_drift_stream}
\alias{sim_drift_stream}
\title{Simulate a binary error stream with a known drift point}
\usage{
sim_drift_stream(
  n_pre = 500,
  n_post = 500,
  p_pre = 0.05,
  p_post = 0.3,
  seed = NULL
)
}
\arguments{
\item{n_pre, n_post}{Number of observations before / after the drift point.}

\item{p_pre, p_post}{Error probability before / after the drift point.}

\item{seed}{Optional integer; if supplied, \code{set.seed()} is called for
reproducibility.}
}
\value{
A tibble with columns \code{t} (index), \code{error} (0/1) and
\code{drift_true} (logical ground truth: \code{TRUE} after the drift point).
}
\description{
Generates a stream of 0/1 classifier errors whose error rate jumps from
\code{p_pre} to \code{p_post} after \code{n_pre} observations. Useful for testing and
validating drift detectors against a known ground truth.
}
\examples{
sim_drift_stream(n_pre = 100, n_post = 100, seed = 42)
}
