% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment.R
\name{augment.drift_detector_fit}
\alias{augment.drift_detector_fit}
\title{Annotated observations from a fitted drift detector}
\usage{
\method{augment}{drift_detector_fit}(x, new_data = NULL, ...)
}
\arguments{
\item{x}{A \code{drift_detector_fit}.}

\item{new_data}{Optional data frame with a new batch to preview.}

\item{...}{Not used.}
}
\value{
A tibble.
}
\description{
With \code{new_data = NULL}, returns the accumulated history (baseline +
advanced batches) annotated with \code{.warning}, \code{.drift} and \code{.phase}.
With \code{new_data}, returns a READ-ONLY preview: the batch annotated from
the current state, WITHOUT persisting it — use \code{\link[=advance]{advance()}} to persist.
}
\examples{
base <- sim_drift_stream(n_pre = 100, n_post = 0, seed = 1)
f0 <- fit(drift_detector("ddm"), base, signal = error)
augment(f0)
}
