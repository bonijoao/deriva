% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_dist_stream}
\alias{sim_dist_stream}
\title{Simulate a continuous stream with a known distribution-shift point}
\usage{
sim_dist_stream(
  n_pre = 500,
  n_post = 500,
  mean_pre = 0,
  mean_post = 3,
  sd_pre = 1,
  sd_post = 1,
  seed = NULL
)
}
\arguments{
\item{n_pre, n_post}{Observations before / after the shift point.}

\item{mean_pre, mean_post}{Means before / after the shift.}

\item{sd_pre, sd_post}{Standard deviations before / after the shift.}

\item{seed}{Optional integer for \code{set.seed()}.}
}
\value{
A tibble with \code{t} (index), \code{value} (numeric) and \code{drift_true}
(logical: \code{TRUE} after the shift point).
}
\description{
Generates a numeric stream drawn from \code{N(mean_pre, sd_pre)} for the first
\code{n_pre} observations and \code{N(mean_post, sd_post)} afterwards. Companion to
\code{\link[=sim_drift_stream]{sim_drift_stream()}} for distribution-based detectors (e.g. \code{"kswin"}).
}
\examples{
sim_dist_stream(n_pre = 100, n_post = 100, mean_post = 3, seed = 42)
}
