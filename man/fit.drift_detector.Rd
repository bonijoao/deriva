% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit.drift_detector}
\alias{fit.drift_detector}
\title{Fit a drift detector on a baseline period}
\usage{
\method{fit}{drift_detector}(object, data, signal, ...)
}
\arguments{
\item{object}{A \code{\link[=drift_detector]{drift_detector()}} specification.}

\item{data}{A data frame with the baseline period, in temporal order.}

\item{signal}{Unquoted name of the signal column (0/1 errors for
error-based methods such as \code{"ddm"}).}

\item{...}{Not used.}
}
\value{
A \code{drift_detector_fit} object.
}
\description{
Runs the detector over the baseline data — the period where the monitored
model is considered stable — so it learns the reference ("normal") level.
The returned object is immutable: feed new batches with \code{\link[=advance]{advance()}}.
}
\examples{
base <- sim_drift_stream(n_pre = 100, n_post = 0, seed = 1)
fit(drift_detector("ddm"), base, signal = error)
}
