% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_drift.R
\name{detect_drift}
\alias{detect_drift}
\title{Detect drift in a signal column (one-shot shortcut)}
\usage{
detect_drift(data, .col, method = "ddm", ...)
}
\arguments{
\item{data}{A data frame in temporal order.}

\item{.col}{Unquoted name of the signal column.}

\item{method}{Name of a registered method (default \code{"ddm"}).}

\item{...}{Hyperparameters forwarded to \code{\link[=drift_detector]{drift_detector()}}.}
}
\value{
\code{data} as a tibble with \code{.warning} and \code{.drift} columns added.
}
\description{
Layer-3 convenience: runs a detector over an existing signal column and
returns the data annotated with \code{.warning} / \code{.drift}. For an explicit
baseline and persistent state, use the full object path:
\code{\link[=drift_detector]{drift_detector()}} + \code{\link[=fit]{fit()}} + \code{\link[=advance]{advance()}}.
}
\details{
For \code{"ddm"}, the warm-up is governed by \code{min_instances}: the first
\code{min_instances - 1} observations get \code{NA} flags.
}
\examples{
s <- sim_drift_stream(seed = 42)
detect_drift(s, .col = error, method = "ddm")
}
